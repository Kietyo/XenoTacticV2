package pathing

import com.xenotactic.gamelogic.model.MapEntity
import com.xenotactic.gamelogic.pathing.Segment

/**
 * Util that augments paths with effects from certain areas.
 *
 * E.g: Augmenting path with speed attributes based on what speed areas it's in.
 */
class PathAugmenter {
    fun augmentSegment(
        segment: Segment,
        area: MapEntity.SpeedArea
    ) {

    }
}